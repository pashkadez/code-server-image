# Code-Server Configuration
# Copy this file to .env and customize the values

# Password for code-server web interface
PASSWORD=        # required — set a strong password

# Password for sudo commands inside container
SUDO_PASSWORD=   # required — sudo password inside container

# Timezone (optional)
TZ=UTC

# Port to expose code-server on
PORT=8080
